# Build output
bin/
obj/
.vs/
*.user
*.suo

# Local config / secrets — never commit keys
*.key
config.json

# CAD artifacts
~$*.sld*
*.SLDPRT~
Models/

# WiX installer build artifacts (FileComponents.wxs is harvested with absolute paths — regenerate, never commit)
installer/FileComponents.wxs
installer/bin/
installer/obj/
/dist/
