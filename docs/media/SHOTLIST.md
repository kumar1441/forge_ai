# Forge media manifest — GitHub README

Files in `docs/media/` and where the README uses them. Each is ≤10 s, 800–1000 px wide, looping, no audio.

## In the README now

| File | README heading | Caption on the page |
|---|---|---|
| `mate-bolts.gif` | (hero, under the tagline) | Mate all the bolts — every fastener classified, seated, then re-verified |
| `mirror.gif` | **Mirror everything except the hardware** | Mirror the assembly across a plane; exclusions previewed before anything changes |
| `fix-red-wave.gif` | **Fix the red wave** | Delta-debugging isolates the single over-defining/dangling mate, removes only it, rebuild goes clean |
| `explode.gif` | **Explode it** | Spread the assembly into a service view along its mate axes; view-only, fully reversible |

## Recording rules (when capturing more)

- Real, complex model only — never a simple flange.
- Keep the verification "✓" card visible in frame — the verify step is the product.
- Trim hard; cut the first ~0.5 s of setup. Prompt typed → change applied → verification card.
